{% extends 'base.html.twig' %}

{% block body %}
    <h1>Liste des articles</h1>
    <ul>
    {% for article in articles %}
        <li><a href="{{ path('articles.show', {'id': article.id}) }}">{{ article.title }}</a></li>
    {% endfor %}
    </ul>
{% endblock %}